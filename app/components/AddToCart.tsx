"use client";
import React from "react";

const AddToCart = () => {
  return (
    <div>
      <button className="btn btn-active btn-neutral" onClick={() => console.log("click")}>Add to Cart</button>
    </div>
  );
};

export default AddToCart;
